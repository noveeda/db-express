extend post_layout 

block content
  .container
    a(href="/board/posts") 
      h1 자유 게시판
    
    // 게시글 정보
    .post-info
      .row
        .label 제목
        .content #{post.post_title}
        .button-container
          form(action=`/board/post/${post.post_id}/edit`, method="get") 
            button.edit-btn(type="submit") 수정하기
      .row
        .label 작성자
        .content #{post.user_nickname}
        .label 작성일
        .content #{post.post_date}
        .label 조회수
        .content #{post.post_views}
    
    // 게시글 내용
    .post-content
      //- HTML 코드 그대로 작동되게끔
      p!= post.post_content 
      br
      br
      br
    
    // 댓글
    .comment-section
      .comment-title 댓글
      .comments
        each comment, index in comments
          .comment
            .comment-writer #{comment.user_nickname}
            .comment-text #{comment.comment_content}
            button.com-delete-btn(onclick="") 삭제
    
      // 댓글 입력
      .comment-input
        //- input.comment-name(type="text" placeholder="닉네임")
        form(action="/comment/:id", method="post") 
          input.comment-content(type="text" placeholder="댓글을 입력해주세요.(100자)")
          button.submit-btn(onclick="") 완료
    
    //- // 하단 버튼
    //- .bottom-buttons
    //-   button.list-btn(onclick="location.href='list.pug'") 목록
    //-   button.board-delete-btn(onclick="") 삭제
